# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository state

This is a course repository for "Lógica y Representación I" (UdeA, 2026-2). It has no build tooling or test suite — it's course planning documents, reference material, and small standalone example scripts:

- `README.md` — full course overview (in Spanish): general info, purpose, learning outcomes, thematic units, summarized schedule, evaluation weights, methodology (flipped classroom + project-based learning), reference bibliography, complementary online courses/judges/gamified platforms, and communication channels
- `2508208_LÓGICA Y REPRESENTACIÓN I.pdf` — official course microcurriculum/syllabus
- `planeacion_tentativa/cronograma.md` — tentative semester schedule (superseded the old `planeacion/cronograma.md`, now removed): session-by-session topics (continuous class numbering 1–48 across 16 weeks), the fixed weekly time slots (Monday lab, mid-week 4h theory block), evaluation weights/dates, and notes on how the unit order and weekly slot order deviate from the official microcurriculum
- `clases/README.md` — real course index, updated through 2026-09-21: a single merged "Sesión magistral" table (one row per lecture, not per clase/tema) covering sessions 1-16 (rows 14-16 added 2026-09-18, alongside the equivalent rows in `clases/7/README.md`'s own hub table), whose **Clase** column links to each `clases/<n>/README.md` hub, and whose **Sesión magistral** column links to each `sesion_magistral-N/README.md`. An earlier pass left two stale leftover sections from a draft "Propuesta" (a CS50-style discussion of how to organize the docs, and a separate by-topic summary table) — those have since been removed; the merged table is the only content now. Manually maintained, so still worth cross-checking against `clases/` directly — e.g. row 7 read "Pendiente por dictar" for weeks even though `sesion_magistral-7/README.md` already had a dated (18/08/2026), substantive writeup (6 ejercicios de repaso de la clase 2 — círculo, nómina, edad en días, hipotenusa, calculadora, corte de uñas — llevados a Python con archivos compartidos por dos estudiantes); fixed 2026-09-21 in both `clases/README.md` (row 7 + the stale note about session 7 lacking a date, itself replaced) and `clases/3/README.md`'s own session table (which previously claimed the class "todavía no se ha dictado" — also stale). Row 8 (`sesion_magistral-8/README.md`) genuinely still has no content beyond **Tipo**/**Fecha**/**Parte**, so it correctly remains "Pendiente por dictar" — a real gap, not a stale one. The date/bloque for session 12 (and the matching row in clase 7's own hub) was previously flagged with a `*` as inferred from script timestamps; that flag and its footnote were removed 2026-09-10 once `sesion_magistral-12/README.md` confirmed the date/bloque directly.
- Per-class folder convention (introduced 2026-08-14, now applied to clases 0-7, naming fully unified 2026-08-14): each `clases/<n>/` has its own `README.md` acting as a CS50-style navigation hub for the topic — a short intro, a **Teoría** section linking to `teoria/README.md` and the slide files, and a **Sesiones magistrales** table listing every lecture that covered the topic with date/modalidad/contenido and a link to its `sesion_magistral-N/README.md`. `teoria/` holds the slides for the whole topic plus that `teoria/README.md` summary; each `sesion_magistral-N/` folder has its own `README.md` stating **Tipo**, **Fecha**, and sometimes **Parte** for which time block of the day. `N` is a *continuous* counter across the whole course, not reset per clase — e.g. clase 2's sessions are numbered 3-6. All session folders follow the single `sesion_magistral-N` pattern (clase 0's `sesion-magistral_1` was the last holdout, renamed 2026-08-14).
- `clases/0/` — Clase 0 "Presentación del curso": `README.md` is the hub (instructor info, course logistics, links to Google Classroom/repo, **plus** a "Sesiones magistrales" section — this clase has no separate `teoria/` folder, the slides *are* the presentation), `clase_00.pdf`/`.pptx` (slides), `diagrama_clase0.drawio` (source diagram), and `sesion_magistral-1/README.md` (session 1, 03/08/2026, presencial)
- `clases/1/` — Clase 1 "Conceptos básicos" / "Conociendo las Herramientas de Trabajo": `README.md` is the hub (tool installs — Anaconda, VS Code, draw.io, PyCharm —, online practice tools, gamified warm-up activities, student checklist, **plus** "Teoría" and "Sesiones magistrales" sections); `teoria/` has the slides (`clase-01.pdf`/`.pptx`: computador, hardware/software, algoritmos, lenguajes de programación) plus its own `teoria/README.md` summary; `sesion_magistral-2/README.md` (session 2, 04/08/2026, presencial, bloque 1)
- `clases/2/` — Clase 2 "Algoritmos y diagramas de flujo (método de Polya)": `README.md` is the hub (intro to the topic, links to `teoria/` and `template_polya/`, table of sessions 3-6); `teoria/` has the slides (`clase-02.pdf`/`.pptx`: método de Polya, variables/tipos de datos, diagramas de flujo, pseudocódigo, 3 ejemplos guiados) plus its own `teoria/README.md` summary, `images/` (shared figures), `template_polya/` (plantilla en blanco del método Polya + `README.md`), and four session folders: `sesion_magistral-3/` (session 3, 04/08/2026, presencial bloque 2, hasta diapositiva 26), `sesion_magistral-4/` (session 4, 10/08/2026, virtual — moved presencial→virtual on short notice; formerly `apuntes_zoom/`, merged into one coherent write-up 2026-08-14), `sesion_magistral-5/` (session 5, 13/08/2026, virtual; formerly `apuntes_zoom_2/`; nómina example), `sesion_magistral-6/` (session 6, 14/08/2026, virtual; brief review session, content deferred to session 7)
- `clases/3/` — Clase 3 "Conceptos básicos de Python": `README.md` is the hub (intro to the topic, links to `teoria/`, table with session 7); `teoria/` has the slides (`clase-03.pdf`/`.pptx`: variables y tipos en Python, cadenas, `print`/`input`, comentarios, 3 ejemplos codificados) plus its own `teoria/README.md` summary, `cadena_udea_index.png`, and `codigo_diapositivas/` (`ejemplo1-area.py`, `ejemplo2-meses.py`, `ejemplo3-circulo.py`, also zipped as `codigo_diapositivas.zip`); `sesion_magistral-7/README.md` is still an empty stub (no Tipo/Fecha/content) — genuinely pending, since that lecture hasn't been given yet, not a docs gap to fix; `clase-03.xopp` is still loose at `clases/3/` root instead of inside `teoria/`
- `clases/4/` — Clase 4 "Operadores y expresiones en Python": `README.md` is the hub (intro to the topic, links to `teoria/`, table with session 8); `teoria/` has `clase-04.pptx` (no PDF yet: operadores aritméticos/relacionales/lógicos, prioridad y asociatividad) plus its own `teoria/README.md` summary; `sesion_magistral-8/README.md` (session 8, 18/08/2026, presencial bloque 2 — scheduled for the Tuesday after the 17 ago holiday)
- `clases/5/` — Clase 5 "Condicionales": `README.md` is the hub (intro to the topic, links to `teoria/`, table with session 9); `teoria/` has the slides (`clase-05.pdf`/`.pptx`: repaso de Polya/operadores, prueba de escritorio, el bloque de decisión, alternativa simple vs. doble, 4 ejemplos guiados) plus its own `teoria/README.md` summary, `teoria/codigo/` (`condicional_simple1-2.py`, `condicional_doble1-2.py`, `ejemplo1.py`, `ejemplo2.py`), and `teoria/diagramas/`+`teoria/images/` (10 matching `.drawio`/`.png` pairs); `sesion_magistral-9/README.md` (session 9, 25/08/2026, presencial bloque 1: prueba de escritorio, condicional simple/doble con el bar de Moe y el subsidio de transporte) ships its own worked-example code and image alongside the README (`ejemplo1_bar_moe_bug.py`, `_if.py`, `_if_else.py`, `ejemplo2_subsidio_if.py`, `_if_else.py`, `prueba_escritorio.py`, `ejemplo_prueba-escritorio.png`)
- `clases/6/` — Clase 6 "Condicionales múltiples": `README.md` is the hub (intro to the topic, links to `teoria/`, table with session 10); `teoria/` has the slides (`clase-06.pdf`/`.pptx`: alternativa múltiple, `if` anidados vs. `if`/`elif`/`else`, 4 ejemplos guiados — bar de Moe y subsidio de transporte retomados de la clase 5 para corregirles bugs, notas de Springfield resuelto de 3 formas, nómina semanal) plus its own `teoria/README.md` summary (structured like clase 5's: "Contenido cubierto" + "Recursos" + a separate "Código de ejemplo" table), `teoria/codigo/` (10 versioned scripts: `ejemplo1_v0-v3.py`, `ejemplo2_v0-v1.py`, `ejemplo3_v1-v3.py`, `ejemplo4.py`), `teoria/diagramas/` (10 `.drawio`) and `teoria/images/` (8 `.png` — not every intermediate version has an exported image); `sesion_magistral-10/README.md` (session 10, 25/08/2026, presencial bloque 2: bar de Moe con validación de entradas + notas de Springfield hasta el programa completo; el subsidio quedó sin analizar y la nómina semanal para la siguiente sesión) ships its own worked-example code alongside the README (`ejemplo1_bar_moe_if_anidados.py`, `_if_elif_else.py`, `ejemplo3_nota_anidado_completo.py`, `_if_elif_else.py`, `test_condicional_nota_anidado.py`, `_if_elif_else.py`); `sesion_magistral-11/README.md` (session 11, 01/09/2026, presencial bloque 1: repaso en el tablero del ejemplo de nómina semanal — cálculo de salario base con horas extra y deducción de impuestos por tramos, resuelto y probado por partes) ships its worked-example code alongside the README (`parte1_salario.py`, `salario_completo.py`)
- `clases/7/` — Clase 7 "Ciclos": `README.md` is the hub (intro to the topic, links to `teoria/`, table now covering sessions 12-16 and a corrected slide count of 91 — fixed 2026-09-18, having previously stopped at session 13 and still said "77 diapositivas"); `teoria/` has the full slide deck (`clase-07.pdf`/`.pptx`, 91 diapositivas: repaso de año bisiesto como puente, componentes de un ciclo, tipos de variables en ciclos, siete ejemplos guiados, y una sección de cierre sobre ciclos infinitos y ruptura de ciclos con `break`; fixed 2026-09-18 to say `Mientras (condición) Haga` instead of the erroneous `Mientras (condición) Entonces` — copy-pasted from the `Si` conditional's keyword — in the three examples that use it, with the same fix carried over to the matching references in `teoria/README.md` and `sesion_magistral-13/README.md`, both of which already used `Haga` everywhere else) plus its own `teoria/README.md` summary (structured like clases 5/6's, with a "Código de ejemplo" table), and a large set of worked examples with matching `.drawio`/`.png` pairs in `teoria/diagramas/`+`teoria/images/` and `.py` scripts in `teoria/codigo/`, covering: año bisiesto (dos formas), impresión de 1 a 10 sin ciclos, el bloque de ciclo (`df_ciclo`), suma de números (dos formas), factorial, cuatro formas del "ejemplo4", bandera vs. centinela ("ejemplo6", dos formas, con un bug intencional dejado como ejercicio), "ejemplo7" (mismo bug), "ejemplo8" — buscar un cero entre `N` números, resuelto en dos formas (`ciclos_ejemplo8_sin_break.py`/`_con_break.py`) — y seis casos de uso comunes de `break` en los ejemplos 9-14 (validación de datos, menú interactivo, lectura hasta un centinela, detección de un dato inválido, límite de intentos, y ciclos anidados donde `break` solo corta el ciclo interno); `sesion_magistral-12/README.md` (session 12, 01/09/2026, presencial bloque 2: primer contacto práctico con ciclos, con pseudocódigo y pruebas de escritorio para `hola_ciclos` — que revela un error off-by-one — y dos formas de generar la serie de números pares hasta N; incluye una sección "Para explorar por su cuenta" con referencias a CS50P Lecture 2 y CS50x Lecture 1 sobre `while`, deliberadamente sin mencionar `for`/`range` porque el curso no lo ha visto aún) ships its worked-example scripts alongside the README (`hola_ciclos.py`, `serie_pares1.py`, `serie_pares2.py`); `sesion_magistral-13/README.md` (session 13, 08/09/2026, presencial bloque 1 (14-16); rewritten 2026-09-10 to document what was actually taught, after an earlier draft had mistakenly duplicated session 12's content verbatim — two parts: **Parte 1** formalizes the three loop components (inicialización/condición/cuerpo) through 6 boundary-case reviews (condition edge cases, instruction order inside the body, infinite-loop risk), cross-linked to `hola_ciclos` from session 12; **Parte 2** builds incrementally a pares/impares counting-and-averaging program illustrating contador/acumulador, whose live testing (via a detailed prueba de escritorio) exposes a division-by-zero bug in the "no hay pares/no hay impares" guard — fixed first with nested `if`, then refactored to `if-elif-else`, with the fix verified against the cases that used to crash) ships its worked-example script alongside the README (`numeros.py`, which still has the original buggy validation logic documented step by step in the README, plus live-coding leftovers — a bare `# print(i)` and a typo'd inline comment — noted inline); `sesion_magistral-14/README.md` (session 14, 08/09/2026, presencial bloque 2 (16-18) — one line: "A esta hora se realizo el primer parcial", i.e. Parcial 1 was administered that block, no new theory; updated 2026-09-18 to link its two shipped PDFs, `logica1_parcial1_2026-2.pdf` — the enunciado — and `logica1_parcial1_2026-2-sol.pdf` — its solución, added the same day); `sesion_magistral-15/README.md` (session 15, 15/09/2026, presencial bloque 1 (14-16) — a repaso of session 13's pares/impares exercise, structured with the same **Parte 1**/**Parte 2** split, both now complete: **Parte 1** reopens the same enunciado/pseudocódigo, adds a quick pseudocódigo↔Python correspondence recap for alternativa simple/doble/múltiple, a mermaid flowchart of the nested-if block faithful to the pseudocódigo's condition order, and compares two Python implementations of that same final block — nested `if` (`impares_v1.py`) vs. `if-elif-else` (`impares_v2.py`) — via three tables (the 4 possible branches — precisely worded so only the first 3 are explicit `elif` conditions and the 4th is the `else` fallthrough by discard, not a fourth evaluated condition —, 4 fresh test cases exercising each branch, and the trace of the nested block itself) plus a tip explaining why deep nesting hurts readability, closing with an explicit note that the pseudocódigo has no `elif`-equivalent (alternativa múltiple is always nested `Si...Sino` blocks per clase 6's theory) so the same pseudocódigo describes both `v1` and `v2`; **Parte 2** changes the enunciado to `N` números `>= 0` (was `positivos, negativos o cero` in an earlier draft, which would have made `-1` an invalid centinela) and covers iteraciones conocidas/desconocidas: a "Repaso rápido" recaps the variable roles (control/contador/acumulador/bandera/centinela, explicitly noting the roles aren't mutually exclusive) and the known-vs-unknown-iterations split via a mermaid decision diagram, then three parallel subsections — contador (`impares_v2.py`), bandera (`impares_v3.py`), centinela (`impares_v4.py`) — each pairing its pseudocódigo with an init/condición/cuerpo mermaid flowchart, a Python fragment, and the traced console output for the same test case (3, 4, 7, 8, 10), closing with a comparative summary table (Estrategia/Cuándo usarla/Variable de control/Condición típica) and a reinforcing note that `while` is the same structure across all three — only the source of the continuation condition changes; an optional "Para explorar por su cuenta" section (not part of what was actually taught) connects CS50P's `while True` + `break` input-validation idiom (contrasted with this session's condition-at-top bandera/centinela, which don't use `break`) and CS50x's `do-while` `meow.c` (forward-linked to the course's still-unseen `Haga` structure, announced as a future topic in the clase 7 theory), plus MIT 6.0001 Lecture 2 as a further-reading link) ships `codigo/impares_v1.py`–`_v4.py`, all four now referenced in the README (`impares_v3.py` originally had a counting bug — the `i` used to number each input prompt was declared but never incremented, so every prompt read "Ingrese el numero 1" — fixed in both the script and the README's Python fragment/trace, verified by re-running the corrected script against the same test case); `sesion_magistral-16/README.md` (session 16, 15/09/2026, presencial bloque 2 (16-18) — reconciled and fully written 2026-09-18 in collaboration with the instructor; the date was corrected from an initially stale 08/09/2026 by cross-checking the `Created on` timestamps embedded in the four `codigo/` scripts, all `Tue Sep 15 2026`, the same technique previously used to infer session 12's date) closes the clase's ciclos block with a "Contenido de esta página" mini table-of-contents (the first session README to need one) and three parts, all built around `ciclos_infinitos.py` and a new Ejemplo-8 variant: **Parte 1** (orden de las instrucciones) reuses that script's one active/uncommented block (suma de 1 a 3) to contrast `i`-before-`suma` vs. `suma`-before-`i` (9 vs. 6), cross-linked to session 13's off-by-one and closing with a `> [!NOTE]` glossary box defining "off-by-one" in Spanish (this prompted the same definition to be retrofitted into session 12 itself — the term's true first appearance in the course — merged into its existing "Para reflexionar" callout rather than as a separate alert box, since that session doesn't otherwise use GitHub-alert-style callouts; session 13's two mentions of the term were turned into links back to that definition); **Parte 2** (ciclos infinitos) walks the script's four commented-out blocks (`while 1`, `while True`, `while i <= 0` with an `else`, `while 5`), explicitly distinguishing the three that are intentional truthy-condition idioms from the `while i <= 0` one, which instead reproduces the "forgot to update the control variable" error pattern (cross-linked to session 13's own Caso 4), plus a `> [!NOTE]` on `while...else` being a Python-only construct with no pseudocode equivalent; **Parte 3** (ruptura de ciclos) retakes teoría's Ejemplo 8 (buscar un cero entre `N` números) and adds, before its two known solutions, a third "sin ruptura" case (`numero_sin_romper_ciclo.py`) that traces the same test input (`N=4`, `0, 13, 1, 8`) against `numero_romper_bandera.py`/`numero_romper_break.py` to show the no-break version both wastes iterations and reports the wrong found-at position — each of the three cases gets its own Mermaid flowchart, with the `break` one visually showing the edge that skips straight to `Fin` without re-checking the header condition; closes with "Para explorar por su cuenta" citing two ellibrodepython.com chapters (`while`, `break`) not used elsewhere in the course, plus a back-reference to session 15's CS50P `while True`+`break` citation and a forward-reference to the still-unseen `Haga`/do-while structure
- `clases/8/` — Clase 8 "Ciclo Para" (el ciclo `for`/`Para` controlado por contador, sucesor del `Mientras`/`while` de la clase 7): `README.md` is now the hub (intro to the topic, links to `teoria/`, no session table yet — an explanatory note says clase 8 hasn't been dictated and will take session number 17, continuing from clase 7's session 16), but there's still no `sesion_magistral-N/` folder, so clase 8 isn't yet indexed in `clases/README.md`'s merged session table or linked from `clases/7/README.md`. `teoria/` has `clase-08.pdf`/`.pptx` (slides, 69 diapositivas: repaso de Polya/condicionales/`Mientras` como puente, el ciclo `Para` — con la nota de que `range()` excluye su límite superior, por lo que el pseudocódigo `Para (vc=inicio,fin,actualizacion)` se traduce a `range(inicio, fin+1, actualizacion)` con paso positivo pero `range(inicio, fin-1, actualizacion)` con paso negativo —, la noción de secuencia en Python, nueve ejemplos guiados, y cierre con ciclos anidados y 5 ejercicios de repaso sin resolver) now has its own `teoria/README.md` summary (structured like clases 5-7's, with a "Código de ejemplo" table) covering: Ejemplo 1 (imprimir 1..N con `for`), Ejemplo 2 (suma de N números, con prueba de escritorio), Ejemplo 3 (factorial, con prueba de escritorio), Ejemplo 4 (secuencia alternante de signos con `range` descendente), Ejemplo 5 (notas de un curso — aprobados/reprobados —, dejado deliberadamente sin validar el rango de la nota), Ejemplo 6 (divisores de un número), Ejemplo 7 en dos formas (contando divisores vs. bandera `es_primo` + `break`), Ejemplo 8 (Fibonacci), y Ejemplo 9 — que extiende el ejemplo 5 con la búsqueda de la nota máxima/mínima, comparando dos formas de inicializar esa variable (Opción 1: valores fijos del dominio, `ejemplo9_opcion1.py`; Opción 2: el primer dato leído, combinada en `ejemplo9_opcion2.py` con `continue` para descartar notas fuera de rango, resolviendo así el bug dejado pendiente en el ejemplo 5). `ejemplo9_continue.py` sigue vacío (stub pendiente para el ejemplo aislado de `break` vs. `continue`), y `teoria/diagramas/`+`teoria/images/` solo cubren los ejemplos 1-8 (nueve pares `.drawio`/`.png` más uno para el diagrama general `ciclo_para` — `ejemplo8.drawio`/`.png` sin el sufijo `_para`, inconsistente con `ejemplo8_para.py`). `teoria/codigo/cine_sr_burns/` es la solución completa (con su propio `README.md`: enunciado, análisis, pseudocódigo, diagrama Mermaid, Python y verificación con prueba de escritorio) del problema repaso del cine del señor Burns planteado sin resolver en la teoría de la clase 7 — sigue usando `while`/`break`, no `for`, por lo que es un repaso de la clase anterior más que contenido nuevo del ciclo `Para`; reorganizado pedagógicamente 2026-09-21 (corrigiendo la tabla de casos de prueba rota, la convención `Sino`/`Escribir`, y una discrepancia entre el enunciado —que hablaba de un centinela `-1`— y la implementación real, que usa un menú con opción 3 para salir).
- `clases/<n>/README.md`, `teoria/README.md`, and `sesion_magistral-N/README.md` files are marked as drafted with generative AI from instructor prompts and reviewed by the instructor; look for the `![Built with AI](...)` badge at the top of a file to tell which ones carry that disclosure — as of 2026-09-06 it's present on `clases/README.md`, all seven `clases/<n>/README.md` hubs, all seven `clases/<n>/teoria/README.md` (clase 0 has no `teoria/`), and every `sesion_magistral-*/README.md` under clases 2, 5, 6, and 7, but missing from others (e.g. `sesion_magistral-1`, `sesion_magistral-2`, `sesion_magistral-3`, `sesion_magistral-7`, `sesion_magistral-8`) — add it when you draft/rewrite a README's content, don't add it to files you only skim or lightly touch
- `laboratorios/` — hands-on lab guides, distinct from the `clases/` lecture material: `laboratorios/README.md` is an index table (Práctica | Tema | Enunciado | Recursos de teoría | Semana | Estado) with three rows — Laboratorio 1 (`laboratorios/1/`, clases 1-4, semana 3, "Publicado"), Laboratorio 2 (`laboratorios/2/`, clases 5-6, semana 5, "Publicado") and Laboratorio 3 (`laboratorios/3/`, clases 7-8, semana 9, "Publicado"; semana counted from week 1 = 03/08/2026, same way as lab 2). `laboratorios/1/README.md` is the actual lab (pair-programming dynamic with Analista/Navegante roles, the *Entender → Diseñar → Predecir → Implementar → Verificar* cycle built around the prueba de escritorio, several exercises each with `> [!Tip]` hint call-outs, and a Python Tutor visualization step at the end); `laboratorios/1/plantilla_metodo_polya_UdeA.docx` is the blank Polya template used during the lab (same template as `clases/2/template_polya/`). `laboratorios/2/README.md` — "Laboratorio 2 — Estructuras condicionales": same pair-programming/Polya methodology as lab 1, 14 exercises across 3 niveles (`if` simple, `if`-`else`, varias alternativas incluyendo anidamiento real), each with casos de prueba tables and most with `> [!Tip]` hints or "Pregunta para pensar" reflection prompts; has its own "Fechas importantes" section (asignación 05/09/2026, entrega y sustentación 19/09/2026) — no separate resource files, just the README. `laboratorios/3/README.md` — "Laboratorio 3 — Ciclos": unlike labs 1-2 it's **individual** (no pair roles) and uses a three-step methodology (Entender → Diseñar a mano → Implementar y verificar) illustrated end-to-end with a worked notas example (plan in words, variable table with a "Rol (si aplica)" column, a first version with `print()` debugging, the final version with those prints commented out, and a casos de prueba table); hand design (variable table, bocetos, prueba de escritorio) is mandatory and part of the paper deliverable. 10 problems in 3 niveles, most with `<details>` "Pista adicional", casos de prueba and "Pregunta para pensar": Nivel 1 — número invertido (`while` + `//`/`%`), cuentas de crédito (centinela `-1`), clasificación de N enteros; Nivel 2 — juego del PUM, rendimiento de combustible (centinela + doble acumulador), serie de Leibniz para π, binario a decimal; Nivel 3 (ciclos anidados) — cos(x) por serie de Taylor (factorial via inner loop, matching clase 8's framing of series as a nested-loop use case), triángulos de asteriscos, tabla de multiplicación combinada. Also has a Python Tutor "Visualización de la ejecución" step, a print-debugging tip, a "Reflexión final", a `> [!NOTE]` telling students the two Code.org "CSA" videos in "Antes de empezar" use **Java** (fundamento over language, with a pseudocódigo/Java/Python comparison table), and an "Anexo: Equivalencias entre pseudocódigo y Python" (A.1 instrucciones básicas, A.2 operadores, A.3 condicionales — noting pseudocode has no `elif` —, A.4 ciclos — incl. the `range()` excluded-stop rule, `fin + 1` vs `fin - 1`), with keywords taken from the clase 7/8 slides. `laboratorios/3/python-cheatsheet.pdf` is the Real Python Pocket Reference, linked from Recursos and the Anexo. Dates: asignación 28/09/2026, entrega y sustentación martes 13/10/2026 (Monday 12/10 is a Colombian holiday). The lab deliberately does **not** cover bandera, `break`/`continue` or búsqueda de extremos (instructor's decision, 2026-09-25). Revised 2026-09-25 after external audits (ChatGPT, Gemini) were triaged — conventions settled then: don't introduce the `math` module (the course never imports modules; square roots use `** 0.5`, as in `clases/3/sesion_magistral-7/.../hipotenusa.py`), format decimals with f-string `:.2f`/`:.6f` (used in class code) rather than `round()`, and reserve "centinela" for a special input value like `-1` — a variable that shrinks until the `while` condition fails is a *variable de control*
- `talleres/` — review workshops meant for exam prep, one per parcial per the `planeacion_tentativa/cronograma.md` weights (Parcial 1 = Algoritmos secuenciales 10% semana 3, Parcial 2 = estructuras de control 20% semana 7, Parcial 3 = POO 15% semana 9, Parcial 4 = arreglos y archivos 20% semana 14): `talleres/README.md` is the index (Taller | Tema | Parcial de repaso | Contenido), currently one row for `algoritmos-secuenciales` → Parcial 1, with a note that talleres for parciales 2-4 will be appended in cronograma order as they're added. `talleres/algoritmos-secuenciales/README.md` — "Taller 1 — Expresiones y Algoritmos Secuenciales", a transcription of docente Carlos Andrés Mera Banguero's original PDF workshop (with an authorship/AI-use disclosure at the top and the `![Built with AI]` badge), reorganized into 3 sections (1. Expresiones, 2. Prueba de escritorio, 3. Algoritmos secuenciales — cross-numbered so exercise labels match their section, e.g. `3.4`), several exercises carrying `> [!Tip]` hint call-outs for concepts a student might need to look up (operator precedence, GB/TB conversion factors, percent-to-decimal, integer-division/modulo carry, law of cosines/sines), plus an "Anexo: Tablas de referencia rápida" (A.1-A.7: I/O/process/decision blocks, arithmetic/relational/logical operators and their priority, método de Polya, and a worked-example guide to the prueba de escritorio format including a ciclo example that previews clase 7 content)
- `material/cheat-sheets/` — quick-reference PDFs for Python and Java-vs-Python comparisons (e.g. `beginners_python_cheat_sheet_pcc_all.pdf`, `mementopython3-english.pdf`, `Java4Python.pdf`, `java_vs_python.pdf`, the Coding Club Python series, `CC-CodeCards.pdf`)
- `material/libros/` — full reference textbooks (PDF) on programming logic and Python: `Joyce-Farrell_Programming-Logic-and-Design.pdf`, `Joyce-Farrell_Introduccion-a-la-programacion.pdf` (renamed from their original accented/special-character filenames), `Florez-Rueda-Roberto_2011_Algoritmia-Basica.pdf`, `Logica-de-Programacion-Efrain-Oviedo.pdf`, `Python Crash Course_...pdf`, `Introduction-to-programming-using-Python-3.pdf`

The `.py` files under `clases/2/` and `clases/3/` are standalone didactic examples (stdlib only, no dependencies, no tests) — run them with `python <archivo>.py`. There is nothing to build, lint, or test at this stage. When more code is added to this repository, update this file with the actual commands and architecture rather than assuming a structure in advance.

`.gitignore` (added 2026-09-13) excludes draw.io's own temporary backup files (`.$*.bkp`), which appear alongside `.drawio` sources whenever a diagram is edited — don't hand-add these to a commit.
